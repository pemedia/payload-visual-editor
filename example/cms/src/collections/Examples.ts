import { CollectionConfig } from "payload/types";

export const Examples: CollectionConfig = {
    slug: "examples",
    fields: [
        {
            name: "someField",
            type: "text",
        },
    ],
}
